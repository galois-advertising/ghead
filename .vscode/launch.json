{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) server", // 项目名字
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/server",
            "args": [""],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb"
        }
    ]
}