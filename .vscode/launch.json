{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) ghead", // 项目名字
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/ghead",
            "args": [""],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb"
        }
    ]
}